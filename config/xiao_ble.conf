# XIAO BLE board-specific configuration for beekeeb dongle
# Display configuration is provided by the dongle_screen shield (zmk-dongle-screen module)

# SPI for ST7789V display (configured by dongle_screen shield)
CONFIG_SPI=y

# Additional dongle_screen options (optional - defaults are sensible)
# CONFIG_DONGLE_SCREEN_HORIZONTAL=y
# CONFIG_DONGLE_SCREEN_DEFAULT_BRIGHTNESS=80
# CONFIG_DONGLE_SCREEN_IDLE_TIMEOUT_S=600
